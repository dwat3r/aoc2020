let test = `1 + 2 * 3 + 4 * 5 + 6`
//part1
let run = input => {
  return function rec(res,input){
    let [h,...tail] = input.split(" ",1)
    if(!isNan(parseInt(h))) {
      
    }
  }(undefined,input)
}